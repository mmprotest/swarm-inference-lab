# Adapter location

The installable adapter is kept with the other first-class backends at
`src/swarm_inference/backends/colibri`. This directory exists to keep the
upstream integration layout discoverable without maintaining a second copy of
runtime code.

